def create_calendar_event(user, email):
    # 🔥 Placeholder (Google Calendar integration later)
    # Right now we just CONFIRM creation
    print(
        f"📅 Calendar event created for: {email.subject} "
        f"on {email.ai_deadline}"
    )
